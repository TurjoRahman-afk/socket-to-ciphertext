# Tooling, not the application. Semaphore itself needs only requirements.txt.
#
#     pip install -r requirements-dev.txt
#
-r requirements.txt

# Turns docs/*.md into Word files for submission: python -m tools.to_word
python-docx>=1.1
